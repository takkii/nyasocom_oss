<ol>
  <% Feedjira.parse(@rss).entries.each do |entry| %>
    <%= entry.summary %><a href='<%= entry.url %>'><%= entry.title %></a><br>
    <br>
    <div class="font-color-skyblue"><%= entry.published.to_time.strftime '投稿時刻 ： %Y年%m月%d日 %H時%M分' %></div><br>
    <br>
  <% end %>
</ol>
